# AlmaLinux 9 Docker Test Suite

## Configure Dockerfile

Update the RPM package to test install

`vi Dockerfile`

## Build

`make build`

## Run the Docker

`make run`

## Enter the Docker

`make bash`

## Test packages and scripts

`cpanp`
